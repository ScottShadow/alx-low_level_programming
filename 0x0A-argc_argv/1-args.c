#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: the number of arguements
 * @argv: the array containing arguements passed
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
