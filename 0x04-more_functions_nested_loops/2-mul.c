#include "main.h"
/**
 * mul - multiplies two integers and returns the result.
 * @a: used to calculate product
 * @b: used to calculate product
 * Return: product of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
